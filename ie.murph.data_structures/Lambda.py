__author__ = 'Paul'

class Lambda(object):

    #Lambda is a nameless function, "x" in this case is a parameter
    def using_lambda(self):
        lambda_one = lambda x: x*7
        print(lambda_one(2))


x = Lambda()
x.using_lambda()

# FINISHED VIDEO 40 OF THE NEW BOSTON PYTHON
